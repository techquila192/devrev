"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const artifacts = __importStar(require("./artifacts"));
const parts = __importStar(require("./parts"));
const timeline = __importStar(require("./timeline"));
const works = __importStar(require("./works"));
const revUsers = __importStar(require("./rev_user"));
test('Test parts SDK functions', () => __awaiter(void 0, void 0, void 0, function* () {
    try {
        yield parts.init();
        var newPartId = yield parts.partsCreateGet();
        yield parts.partsUpdateGet(newPartId);
        yield expect(parts.partsDeleteGet(newPartId)).rejects.toThrow();
        yield parts.partsList();
    }
    catch (error) {
        console.log(error);
    }
}), 50000);
test('Test works SDK functions', () => __awaiter(void 0, void 0, void 0, function* () {
    try {
        yield works.init();
        var id = yield works.worksCreateGet();
        yield works.worksUpdatedGet(id);
        yield works.worksExport();
        yield works.worksList();
        yield expect(works.worksDeleteGet(id)).rejects.toThrow();
        yield works.cleanup();
    }
    catch (error) {
        console.log(error);
        yield works.cleanup();
    }
}), 30000);
test('Test artifacts SDK functions', () => __awaiter(void 0, void 0, void 0, function* () {
    try {
        var id = yield artifacts.artifactPrepare();
        yield artifacts.artifactLocate(id);
    }
    catch (error) {
        console.log(error);
    }
}));
test('Test timeline SDK functions', () => __awaiter(void 0, void 0, void 0, function* () {
    try {
        yield timeline.init();
        var id = yield timeline.timelineCreateGet();
        yield timeline.timelineUpdateGet(id);
        yield timeline.timelineList();
        yield timeline.cleanup();
    }
    catch (error) {
        console.log(error);
        yield timeline.cleanup();
    }
}), 50000);
test('Test rev_user SDK functions', () => __awaiter(void 0, void 0, void 0, function* () {
    try {
        var id = yield revUsers.revUsersCreateGet();
        yield revUsers.revUsersUpdateGet(id);
        yield revUsers.RevUsersList();
        yield revUsers.revUsersDeleteGet(id);
    }
    catch (error) {
        console.log(error);
        revUsers.cleanup();
    }
}), 30000);
